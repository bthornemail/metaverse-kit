# @metaverse-kit/protocol

Defines the core event envelope and operation types for the entire system.

Key responsibilities:
- Event envelope fields and invariants
- Operation-specific event types
- Validation helpers (`validateWorldEvent`, `validateWorldEvents`)
- Proposal and acceptance events (SPABBS)
