# @metaverse-kit/addr

Content addressing for immutable data (RID/SID).

Key responsibilities:
- Canonical JSON serialization
- Deterministic hashing
- RID (content ID) and SID (structural pointer) helpers
