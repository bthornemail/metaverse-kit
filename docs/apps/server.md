# apps/server

HTTP/WebSocket API for tile sync, event ingestion, and discovery.

Key responsibilities:
- Serve tile tips, segments, and objects
- Append event ingestion with validation
- Presence WebSocket
- EXT32 pack registry endpoints (`/ext32/packs`)
