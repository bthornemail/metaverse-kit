# demo-portal-eval

Runs deterministic evaluation checks for the demo portal slice.

Script:

- `scripts/demo-portal-eval.sh`

Checks:

1. deterministic replay test
2. integrity failure test (fail closed)
3. proposal export test
4. performance trace output

Run:

```bash
bash scripts/demo-portal-eval.sh
```
